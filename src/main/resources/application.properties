spring.application.name=backend__pasteleria

# Configuración de la base de datos H2 (localStorage simulation)
# Código MySQL inutilizado - ahora usa H2

# Nueva configuración H2
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# Configuración de JPA/Hibernate para H2
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Habilitar consola H2 para desarrollo
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JWT (inutilizado pero conservado)
# jwt.secret=superSecretKeyThatShouldBeAtLeast256BitsLongForHS256Algorithm
# jwt.expiration=86400000

